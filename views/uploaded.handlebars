<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title>Backbone beginnings</title>
    </head>
    <body>
        {{#if file}}
        <img src="{{file}}"></img>
        {{else}}
        <p color="red">Error, Try again</p>
        {{/if}}
        <button><a href="/upload">Upload another</a></button>
    </body>
</html>
